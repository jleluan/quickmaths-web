// Placeholder for advanced selection logic. The GameModel currently
// implements a basic version of block selection and validation. This
// module will hold more nuanced behaviours such as handling combos,
// chained selections and highlighting rules.

export function validateSelection(sum: number, targetValue: number): boolean {
  return sum === targetValue;
}
