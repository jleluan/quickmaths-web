{
  "name": "quickmaths-web",
  "version": "0.1.0",
  "private": true,
  "description": "Modern browser rebuild of QuickMaths using TypeScript, Phaser 3, and Vite. Faithful replication of the original game logic.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.2.0"
  }
}